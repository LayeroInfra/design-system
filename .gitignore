node_modules/
storybook-static/
dist/
*.log
.DS_Store
src/**/*.js

# Привязка CLI к проекту — состояние конкретной машины, не репозитория.
# Workflow целится в проект явным `project:`, так что файл здесь не нужен.
.layero/
